apiVersion: v1
kind: Service
metadata:
  name: users-s-c
  labels:
    app: users
spec:
  type: ClusterIP
  selector:
    app: users
  ports:
    - name: http
      protocol: TCP
      port: 50052
      targetPort: 50052